# AUTOPSIE — GOLD# ticket 377299478 (SELL, -$42.85)

**Note méthodologique préalable** : les horodatages de `logs/hermes.log` sont en heure LOCALE machine (UTC+1), et les `deal.time` bruts renvoyés par MT5 sont en heure murale broker (UTC+3) — établi et vérifié plus tôt dans cette session (`app/utils/broker_time.py`). Tous les horodatages ci-dessous sont normalisés en UTC VRAI ; l'heure locale/broker équivalente est donnée entre parenthèses au premier usage. Les horodatages "18:30:49" / "20:50:20" cités dans la mission sont les lectures brutes MT5 (broker, UTC+3) — en UTC vrai cela donne 15:30:49 / 17:50:20.

## 1. La décision d'entrée

- **Stratégie émettrice** : `ORDER_FLOW_EXECUTION_AGENT`, décision à **2026-07-08T15:30:46.641 UTC** (16:30:46 local), setup `ORDER_FLOW_SELL_VWAP_RECLAIM_REJECTION`, score interne 93/100, grade A sur sa propre grille.
- **Contexte marché** : session `OVERLAP`, mercredi (`WEDNESDAY` — confirme l'hypothèse de la mission, week-end non applicable), `kill_zone_active=false`, régime `atr_percentile=47.0` (médian). H4/H1 lus en bearish dans le narratif top-down ("D1 is bearish, H4 is bearish, H1 is bearish with bos_choch structure. M15 confirms direction.") mais `regime.h4_bias` n'est pas persisté dans cette ligne du dataset — la trace log séparée (`MTF_DIR`) confirme `h4_bias=BEARISH h4_zone=SUPPORT`, `H1_BIAS` lui-même `NEUTRAL, aligned=False`.
- **Confluence** : score final **63.42 (grade C)**, mode `ADAPTIVE`, seuil symbole 58.0 → **passe** (`confluence_threshold_pass=true`, raison `GOLD_ADAPTIVE_THRESHOLD`). Pas de décomposition geo/smc/mtfa/of dans cette ligne précise du dataset, mais les gates individuels sont visibles : `mtfa_score=15.0 (FAIL)`, `smc_score=50.0 (FAIL)`, `order_flow_execution_agent_score=93 (signal SELL)` — **c'est la composante order-flow qui a fait passer ce trade**, MTFA et SMC étaient tous deux en échec.
- **EES** : non trouvé sous ce nom dans le dataset ni dans les logs autour de l'entrée — soit non calculé pour ce chemin de décision (`ORDER_FLOW_EXECUTION_AGENT` a sa propre grille de score, séparée du pipeline EES), soit non persisté. Pas de preuve directe d'une pénalité ou d'un bonus EES-SELL sur cette entrée précise.
- **momentum_alignment** : `alignment=ALIGNED`, `consensus=BEAR` (2 votes bearish sur 3 : `vote_cvd_slope=-1`, `vote_delta=-1`, contre `vote_m1_last5=+1`) — le SELL était **ALIGNÉ** avec le momentum consensus, pas à contre-courant.
- **Les 7 sens** :
  - DXY : `trend=UP`, `change_h1=+0.077`, `change_m15=+0.019`, `divergence=NORMAL_INVERSE` — DXY qui monte est structurellement baissier pour l'or (relation inverse), **cohérent avec un SELL**.
  - Niveaux D1/W1 : prix à seulement **37 points (0.004 ATR) du plus bas hebdomadaire** (`week_low=4026.20`, entrée à 4026.27) — le SELL a été pris quasiment SUR le plus bas de la semaine, ce qui est plutôt un signal de continuation de faiblesse qu'un rebond anticipé.
  - Canari minières (HUI) : **indisponible** (`eyes_hui_available=false`, raison `MINER_SYMBOLS_ABSENT`) — ce sens n'a pas pu voter sur cette entrée.
  - CVD / order flow : `divergence=bear`, `cvd_session=-383`, mixte sur les pentes intraday (`slope_m15=+63`, `slope_m5=+19`) mais la lecture globale servant à la décision reste bearish.
  - Proxy whale/smart-money (WSP) : unanimement bearish-cohérent — `market_state=BEARISH`, `mm_sentiment=DISTRIBUTION`, `volume_pressure=BEARISH (-29.3%)`, `trap_check=VALID`, `htf_trend_alignment=ALIGNED`.
- **Verdict section 1** : entrée **défendable mais pas un setup A/B "propre"** — confluence globale grade C (63.42, tout juste au-dessus du seuil adaptatif 58), MTFA et SMC en échec, mais order-flow fort (score 93) et momentum aligné. Point le plus important : ce trade est explicitement tagué `mode=DEMO_MICRO_DISCOVERY`, `micro_discovery_reason=MICRO_DISCOVERY_CONFLUENCE_SAMPLE`, `exploration_decision=ALLOW` — **le système savait lui-même qu'il s'agissait d'un setup marginal échantillonné délibérément en mode exploration**, pas d'une entrée haute-conviction. Ce n'est pas un setup qui "a triché" les gates : c'est le comportement voulu du mode exploration (échantillonner des setups moyens pour construire de la donnée d'edge).

## 2. La géométrie SL/TP

- SL 4162.55 / TP 3821.85 / entrée 4026.27 (signal) — 4024.17 (fill réel, léger slippage favorable de 2.10 points).
- **Le SL n'est PAS un multiple d'ATR** pour ce type de setup : `OF_SLTP` (le sous-module SL/TP propre à `ORDER_FLOW_EXECUTION_AGENT`) le place par rapport à la géométrie de la value area, pas par rapport à l'ATR. Preuve : `order_flow_execution_agent.vah=4158.52` (Value Area High) — le SL à 4162.55 est à seulement **4.03 points au-delà du VAH**, un buffer de sécurité classique en trading order-flow (stop placé juste au-delà de la zone de valeur, pas un multiple ATR). C'est une méthodologie différente, structurellement justifiée, pas un SL "mal dimensionné" par erreur.
- Deux lectures d'ATR trouvées dans les logs au même moment (échelles différentes, non réconciliées dans cette investigation) : `ATR_AUDIT atr=8.16` (source=candles, probablement M1) et `GOLD_RANGE_STATUS atr=40.29` (probablement M15/H1). Ni l'une ni l'autre n'est directement la base du calcul SL de ce setup — c'est bien le VAH qui l'explique.
- RR : `rr=1.5` / `net_rr=1.5` dans le dataset (contre ~1.46 calculé dans la mission avec le prix d'entrée signal vs fill — écart mineur d'arrondi de prix).
- **Risque réel** : `risk_pct=1.497%` — **très en dessous** du cap C4 (10%) mentionné dans la mission, donc passage normal du risk check, pas une anomalie. `kelly_blocked_reason=MIN_LOT_EXCEEDS_RISK` : le lot Kelly optimal aurait été inférieur à 0.01 (le minimum MT5), donc le trade est mécaniquement un peu plus risqué en % que ce que Kelly aurait choisi si un lot plus petit était possible — limite structurelle du lot minimum, pas un bug.

## 3. La vie du trade (2h20 de tenue)

- **MFE (max favorable)** : **+$0.97** à 15:32:50 UTC (16:32:50 local), soit ~2 minutes après l'entrée — jamais assez pour armer le seuil BE ($2.00).
- **MAE (max adverse)** : **-$51.65** à 17:31:26 UTC (18:31:26 local), soit ~19 minutes avant la clôture — le trade était donc PLUS mauvais qu'à la clôture, et a partiellement récupéré (+$8.80) dans les minutes précédant la fermeture.
- **Exit V2** : jamais armé (`be_armed=False` sur les 323 évaluations), jamais de `TRAIL_MOVE` (le pic de $0.97 n'a jamais atteint le seuil de trailing à $2.00), et **zéro action CLOSE loguée par Exit V2 lui-même** sur ce ticket — confirmé programmatiquement (0/323 évaluations avec action=CLOSE).
- Le prix a effectivement atteint la zone de -$51 sans jamais s'approcher du SL (4162.55) — cohérent avec l'observation de la mission.

## 4. La fermeture — la question clé, résolue

**Qui a fermé** : le mécanisme `NEWS_PRECLOSE`, PAS Exit V2, PAS le SL/TP broker, PAS une fermeture manuelle. Preuve directe dans `logs/hermes.log` :

```
2026-07-08 18:50:18,083 [INFO] [NEWS_PRECLOSE] ticket=377299478 decision=CLOSE
reason=NON_ARMED_BEFORE_MAJOR event=FOMC Meeting Minutes event_time_utc=2026-07-08 18:00:00+00:00
```
(18:50:18 local = 17:50:18 UTC vrai)

- L'événement `FOMC Meeting Minutes` était programmé à **18:00:00 UTC vrai**. La fermeture a eu lieu à **17:50:18 UTC vrai, soit 9 minutes 42 secondes avant** — dans la fenêtre de préclôture configurée (`news_preclose_window_minutes=10`, `app/config.py:125`).
- La condition de déclenchement exacte (`app/mt5/demo_router.py`, bloc `NEWS_PRECLOSE` dans `process_quick_exits`) : une position magic=909002 non armée (`_position_armed(pos)` retourne False) est fermée si un événement majeur est détecté dans les `news_preclose_window_minutes` à venir. Ce trade n'a jamais été armé (section 3) → condition remplie.
- Le deal de fermeture MT5 (`deal ticket=375800497`, `reason=EXPERT`, `comment=HERMES_QUICK_EXI` — troncature MT5 16 caractères de `HERMES_QUICK_EXIT_TP`, le commentaire générique du helper de fermeture partagé `_quick_exit_close`, réutilisé par plusieurs mécanismes dont Exit V2 ET NEWS_PRECLOSE) confirme une fermeture EXPERT (bot), pas SL/TP broker (`reason` aurait été 4 ou 5), pas client manuel (`reason` aurait été 0).
- **Ce n'est ni un bug ni un comportement inattendu** : c'est le garde-fou "ne jamais tenir une position non protégée à travers un événement macro majeur" qui a fonctionné exactement comme conçu. La mission avait elle-même évoqué cette piste ("NEWS_FLAT (FOMC ? mais c'était à 18h UTC)") et l'avait provisoirement écartée en comparant à l'heure broker brute (20:50) sans la corriger — la correction UTC vrai (17:50, 10 minutes avant 18:00) confirme l'hypothèse.

## 5. Verdict et leçon

- **Classification** : (a) un setup moyen (grade C confluence, marginal mais order-flow fort et momentum aligné), pris délibérément en mode exploration (`MICRO_DISCOVERY_CONFLUENCE_SAMPLE`), qui n'a jamais vraiment décollé (MFE $0.97) et a dérivé contre la position — **pas malchanceux au sens d'un bon setup trahi par le marché, plutôt un échantillon d'exploration qui n'a pas fonctionné**, ce qui est le résultat statistiquement attendu d'un mode conçu pour tester des setups moyens.
- La fermeture n'est PAS anormale — c'est une protection qui a fait exactement son travail (fermer une position non armée avant un risque macro majeur).
- MFE/MAE : jamais favorable au-delà de $0.97, MAE la plus profonde 19 minutes avant la clôture (-$51.65), récupération partielle avant la fermeture protective.
- **Rien à corriger** : le SL est structurellement justifié (au-delà du VAH, pas un multiple ATR arbitraire), le risk check est passé normalement (1.5% << cap 10%), la fermeture NEWS_PRECLOSE a fonctionné selon sa conception. Aucune recommandation RÉSERVÉ SIMO — pas de seuil ni de logique à changer.

## Bottom-line (6 lignes)

Ce trade a été pris par `ORDER_FLOW_EXECUTION_AGENT` (score 93, VWAP reclaim rejection) sur un setup de confluence globale moyenne (grade C, 63.42/100) explicitement échantillonné en mode exploration (`MICRO_DISCOVERY_CONFLUENCE_SAMPLE`), avec momentum aligné et DXY haussier cohérent avec un SELL. Le SL de 138 points n'est pas un multiple d'ATR mal calibré : il est placé 4 points au-delà du VAH (Value Area High), une méthodologie order-flow standard, et le risque réel (1.5% de l'équité) était très en dessous du cap de 10%. Le trade n'a jamais dépassé +$0.97 de profit flottant (jamais armé, jamais protégé par Exit V2) et a atteint -$51.65 dix-neuf minutes avant sa clôture, remontant à -$42.85 au moment de la fermeture. Ce n'est PAS Exit V2, ni le SL/TP broker, ni une fermeture manuelle qui a clos la position : c'est le garde-fou `NEWS_PRECLOSE`, qui a fermé la position non armée 9 minutes 42 avant la publication FOMC Meeting Minutes (18:00 UTC), exactement dans sa fenêtre de conception (10 minutes). C'est un comportement système normal et protecteur, pas une anomalie. **Verdict : perte normale d'un échantillon d'exploration à confluence moyenne, correctement risk-managée à la sortie — rien à corriger.**
