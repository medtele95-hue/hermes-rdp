# AUTOPSIE — GOLD# ticket 379832386 (BUY, −$20.08)

**Note méthodologique** : tous les horodatages ci-dessous sont normalisés en **UTC vrai**, avec l'heure locale machine (UTC+1) ou broker (UTC+3) donnée entre parenthèses au premier usage — convention établie `app/utils/broker_time.py`, déjà validée dans l'autopsie précédente (`AUTOPSIE_377299478.md`). Trois sources indépendantes ont été croisées : le dataset (`decision_dataset.jsonl`, lignes `decision` et `outcome`), le log (`hermes.log`, 623 évaluations `[EXIT_V2]` sur ce ticket, aucun trou), et l'historique des deals MT5 (requête read-only isolée, `tools/autopsy_379832386.py`, jamais de `order_send`, jamais d'écriture).

## ① Le trade en 5 lignes

- **GOLD# BUY 0.01 lot**, ticket 379832386, magic 909002, stratégie `ORDER_FLOW_EXECUTION_AGENT`, mode `DEMO_ADAPTIVE_FALLBACK`.
- Ouvert **2026-07-10T04:13:07 UTC vrai** (07:13:07 broker) — fill à 4119.74 (0.39 pt de slippage défavorable vs le prix signal 4119.35).
- SL 4099.68 · TP 4148.86.
- Fermé **2026-07-10T08:37:11 UTC vrai** (11:37:11 broker) à 4099.66 — **SL touché proprement** (confirmé, section ④).
- **−$20.08**, 4h24 de vie, 623 évaluations Exit V2 sans le moindre trou de log.

## ② L'entrée — pourquoi le système a pris ce BUY

- **Décision** : `PASS`, confluence finale **60.02 (grade C)** ≥ seuil symbole 58.0 → passe (`confluence_threshold_pass=true`, raison `GOLD_ADAPTIVE_THRESHOLD`).
- **Signal émetteur** : `ORDER_FLOW_EXECUTION_AGENT`, signal BUY, score 88/100, raison `ORDER_FLOW_BUY_VALUE_AREA_ROTATION`.
- **Setup global** : grade **B** (`gate_statuses.setup_grade`) mais **MTFA en échec** (score 40, FAIL), **SMC en échec** (score 20, FAIL), **top-down `WAIT_FOR_CONFIRMATION`** (score 44, `m15_confirmation_pass=false`) — un setup **marginal**, pas un A/B propre.
- **momentum_alignment** : `alignment=AGAINST`, `consensus=BEAR` (2 votes bear sur 3 : `vote_cvd_slope=-1`, `vote_m1_last5=-1`, contre `vote_delta=+1`) — **ce BUY allait à CONTRE-COURANT du consensus momentum du système lui-même**, un facteur défavorable notable.
- **DXY** : `trend=UP` (+0.018 sur H1) — un DXY haussier est structurellement baissier pour l'or, donc **pas cohérent** avec un BUY. Deuxième facteur défavorable.
- Session `ASIA_MAIN`, `kill_zone_active=false`, `regime.atr_percentile=30.3` (régime plutôt calme, médiane du dataset GOLD).
- **EES** : aucun champ EES_buy trouvé sur cette ligne de décision — comme dans l'autopsie précédente, `ORDER_FLOW_EXECUTION_AGENT` utilise sa propre grille de score, séparée du pipeline EES.

### Le point RR (le point le plus intéressant de l'autopsie)

- **RR loggé par le système = 1.5 exactement** (`rr`/`net_rr`), calculé à partir du **prix signal théorique** (entrée 4119.35, SL 4099.67782, TP 4148.85827) : `(4148.85827−4119.35) / (4119.35−4099.67782) = 29.50827 / 19.67218 = 1.5000...` — exact, pas un arrondi.
- **RR recalculé avec le prix de FILL réel** (4119.74, après slippage) : SL à 20.06 pts, TP à 29.12 pts → **RR réalisé ≈ 1.452** — confirme le calcul de la mission (≈1.45). L'écart vient uniquement du slippage d'exécution (SL/TP restent fixés à leurs niveaux de prix absolus calculés au moment du signal, ils ne sont pas recentrés sur le prix de fill réel).
- **Il existe bien un gate RR≥1.5 dans le code — mais il n'est PAS universel.** `app/agents/kelly_risk_agent.py:50-51` :
  ```python
  if rr < 1.5:
      blocked.append("REWARD_RISK_BELOW_1_5")
  ```
  Avec `kelly_reward_risk=1.4999999999999307` (imprécision flottante, juste sous 1.5), ce gate **s'est bien déclenché** : `kelly_blocked_reason="REWARD_RISK_BELOW_1_5"` dans le dataset le confirme — **Kelly a bloqué ce trade de son point de vue**.
- **Mais ce blocage Kelly n'a jamais empêché l'exécution**, parce que ce trade est passé par un chemin totalement différent : `mode=DEMO_ADAPTIVE_FALLBACK`, `exploration_override_reason=ADAPTIVE_CONFLUENCE_FALLBACK`. La fonction qui a réellement autorisé ce trade, `_demo_topdown_fallback_review` (`app/mt5/demo_router.py:2018-2076`), **ne vérifie le RR nulle part** — elle ne gate que sur : le mode démo/connexion MT5 (`_demo_topdown_fallback_hard_block_reason`), le seuil de confluence adaptative (`_demo_topdown_fallback_adaptive_block_reason`, ≥58 et ≥55), et la confirmation de déclenchement M15/M1/score≥65 pour GOLD (`_demo_topdown_fallback_trigger_block_reason`). Le champ `kelly_blocked_reason` n'est enregistré dans le dataset (`demo_router.py:1390`) que pour **observabilité** — il n'est **consulté nulle part** dans la logique ALLOW/BLOCK du chemin fallback.
- **Verdict RR : CONFIRMÉ, mais ce n'est pas un contournement caché ou un bug** — c'est un comportement cohérent et reproductible, par construction : le chemin `DEMO_ADAPTIVE_FALLBACK` (comme `DEMO_MICRO_DISCOVERY`, déjà documenté dans l'autopsie précédente pour le ticket 377299478) **n'a jamais consulté le gate RR≥1.5 de Kelly**. Kelly gate uniquement le dimensionnement dynamique du chemin **strict**, pas les chemins d'exploration/fallback démo, qui utilisent un lot plancher fixe (0.01) et leurs propres critères (confluence + déclenchement). **À noter pour la watch-list : le gate RR≥1.5 n'est pas une garantie universelle sur tous les trades DEMO, seulement sur le chemin Kelly strict.**

### Le SL

- 20.06 pts (réalisé) / 19.67 pts (théorique) — cohérence avec l'ATR : **ATR H1(14) descriptif ≈ 12.90 pts** (calcul standard fait pour ce rapport uniquement, PAS la formule interne du système — aucun champ `atr` brut n'est persisté sur cette ligne du dataset, seul `regime.atr_percentile=30.3` l'est). `sl_dist_atr ≈ 1.53–1.56×ATR` — un multiple raisonnable, ni anormalement serré ni large.

## ③ La vie du trade (04:13 → 08:37 UTC vrai) — Exit V2 a-t-il eu sa chance

- **MFE (source autoritaire : ligne `outcome` du dataset, `mfe=1.86` pts)** — recoupé indépendamment par le log Exit V2 (623 évaluations sans trou) : **pic de profit flottant = +$1.31 à 04:17:58 UTC vrai**, ~5 minutes après l'entrée. Les deux sources convergent : le trade n'a **jamais dépassé environ $1.3–1.9** de profit flottant.
- **be_armed est resté `False` sur les 623 évaluations, sans exception** — parce que le seuil d'armement (`be_arm_usd_effective=2.0`) **n'a jamais été atteint** : le pic de $1.31 ne représente que 65.5% du chemin vers l'armement.
- **Zéro action Exit V2 ≠ `NONE`** sur ce ticket, confirmé par recherche exhaustive du log (aucune ligne `action=` autre que `NONE`) — jamais de `TRAIL_MOVE`, jamais de `CLOSE` émis par Exit V2 lui-même.
- **MAE** : le trade s'est dégradé progressivement et n'a plus jamais approché le seuil d'armement après son pic initial. Dernières lectures avant clôture : −$17.37 (08:34:40 UTC vrai) puis −$17.32 (08:35:58), la position continuant de se détériorer dans les ~90 dernières secondes jusqu'à toucher le SL à −$20.08.
- **Verdict section ③ : Exit V2 n'a RIEN à se reprocher.** Le prix n'est jamais monté assez haut pour que la protection breakeven ait ne serait-ce qu'une chance de s'armer — ce n'est pas Exit V2 qui a raté une opportunité de protéger le trade, l'opportunité n'a simplement jamais existé. Comportement système strictement normal.

## ④ La sortie — confirmation SL propre

- **Deal MT5 de clôture** (requête isolée lecture-seule, `tools/autopsy_379832386.py`) : deal `378309658`, `entry=1` (OUT), **`reason=4`** (code broker `DEAL_REASON_SL`), prix **4099.66**, commentaire `'[sl 4099.68]'`, P&L **−$20.08**.
- Écart 4099.68 (SL nominal) → 4099.66 (prix d'exécution) = **0.02 pt** — slippage/spread normal, sans particularité.
- **Confirmé : c'est un `BROKER_SL` propre** — pas une fermeture logicielle (Exit V2 ou `NEWS_PRECLOSE` auraient `reason=3`/`EXPERT`, comme dans l'autopsie du ticket 377299478), pas une fermeture manuelle (`reason=0`/`CLIENT`).
- Cross-check signe/valeur : P&L deal MT5 = −$20.08, cohérent avec la mission et avec `pnl_reconciled=-20.08` dans le dataset (`pnl_source=MT5_HISTORY_DEALS`).

## ⑤ Le contexte (descriptif, pas de jugement)

- **sl_dist_atr** : `regime.atr_percentile` de ce trade = 30.3, sur une distribution GOLD de 2593 lignes de décision allant de 0.3 à 74.6 (médiane 30.3) — **ce trade est pris exactement à la médiane du régime de volatilité observé**, ni un jour anormalement calme ni anormalement volatil.
- **Répartition GOLD par session** (35 sorties réelles au total dans le dataset entier, tous symboles ; hors échantillons virtuels/shadow qui gonflaient artificiellement le compte — filtré explicitement via `virtual=false`) :

  | session | N | win rate | P&L cumulé |
  |---|---|---|---|
  | ASIA_MAIN | 10 | 70.0% | −$8.05 |
  | LONDON | 6 | 66.7% | −$17.07 |
  | OVERLAP | 4 | 75.0% | −$51.31 |

  **Descriptif seulement, n trop petit pour conclure quoi que ce soit** (10 trades Asie dans tout le dataset) : le win rate Asie n'est pas plus faible qu'ailleurs — au contraire légèrement plus bas que Londres/Overlap mais avec un P&L cumulé bien moins négatif, cohérent avec des pertes individuelles plus petites en Asie. Rien qui isole la session Asie comme facteur explicatif de cette perte précise.
- **Les pertes BTC du même jour (2026-07-10)** : le dataset contient **3 pertes BTC réelles** ce jour (tickets 379852497 −$0.64, 379991054 −$0.70, 380041191 −$0.57) sur 5 trades BTC réels au total (2 gagnants : +$0.26, +$1.73) — la mission en mentionnait 4 ; seules 3 apparaissent dans le dataset au moment de cette lecture (écart mineur non creusé, hors périmètre de cette autopsie ciblée sur GOLD 379832386). **Confirmé en une ligne : les 3 pertes BTC trouvées suivent bien le pattern déjà connu** — aucune des trois n'a jamais eu `be_armed=True` dans le log Exit V2 (recherche exhaustive, 0 occurrence), cohérent avec le chantier déjà noté (rien de neuf).

## Verdict (3 phrases max)

**Perte normale par conception, avec une anomalie architecturale confirmée mais non nouvelle, déjà documentée pour la watch-list.** Ce trade est un setup marginal (grade C confluence, MTFA/SMC en échec, momentum et DXY tous deux défavorables au BUY) passé par le chemin `DEMO_ADAPTIVE_FALLBACK`, qui a ignoré le blocage RR<1.5 de Kelly parce que ce chemin ne consulte jamais ce gate par construction (`app/mt5/demo_router.py:2018-2076` vs `app/agents/kelly_risk_agent.py:50-51`) — un comportement système cohérent et reproductible, pas un bug ponctuel ni un contournement caché. Le prix n'est jamais monté assez haut pour armer Exit V2 (pic +$1.31 sur $2.00 requis, confirmé par deux sources indépendantes) puis a dérivé jusqu'à un SL broker propre (0.02 pt de slippage) : rien à corriger dans ce trade précis, mais **le fait que les chemins fallback/exploration démo n'appliquent jamais le gate RR≥1.5 mérite d'être noté explicitement dans la watch-list système**, sans y toucher pendant le gel.

---

*Script isolé utilisé pour cette autopsie : `tools/autopsy_379832386.py` (lecture seule, jamais de `order_send`, `mt5.initialize()`/`mt5.shutdown()` dans un process court-vécu isolé, ne touche ni le bot ni le dataset).*
